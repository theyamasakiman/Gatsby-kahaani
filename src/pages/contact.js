import * as React from 'react'
import { Link } from 'gatsby'
import Layout from '../components/layout'

const TeamPage = () => {
  return (
    <Layout pageTitle={"Contact"}>
      <p>Hire us we need money.</p>
    </Layout>
  )
}

export default TeamPage